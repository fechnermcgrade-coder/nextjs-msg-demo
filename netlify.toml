[build]
  command = "corepack pnpm install --frozen-lockfile && corepack pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "9.15.4"

[[plugins]]
  package = "@netlify/plugin-nextjs"
